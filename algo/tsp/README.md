Traveling Salesman Problem using Bitmaps

- Itervative version computing all subsets of graph nodes

Run after compilation using

```
./k < tests/tsp-1.in
```

and compare output with `tests/tsp-1.out`.
